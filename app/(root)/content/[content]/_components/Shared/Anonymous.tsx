import { Button } from '@/components/ui/button'
import React from 'react'

type Props = {}

const Anonymous = (props: Props) => {
  return (
    <Button>
        Create Account
    </Button>
  )
}

export default Anonymous